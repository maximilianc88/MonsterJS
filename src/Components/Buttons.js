import React from "react";

const Button = (props) => {
  return (
    <div>
      <div className="comp-cat buttons">
        <h1> Buttons:</h1>
      </div>
    </div>
  );
};

export default Button;
