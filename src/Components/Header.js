import React from "react";

const Header = (props) => {
  return (
    <div>
      <div className="comp-cat headers">
        <h1> Headers:</h1>
      </div>
    </div>
  );
};

export default Header;
